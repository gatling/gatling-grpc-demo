plugins {
  id 'application'
  id 'idea'
  id 'java'
  id 'com.google.protobuf'   version '0.9.4'
  id 'com.diffplug.spotless' version '6.20.0'
}

repositories {
  mavenCentral()
}

dependencies {
  compileOnly 'javax.annotation:javax.annotation-api:1.3.2'
  implementation 'io.grpc:grpc-netty-shaded:1.57.0' // shaded: includes ssl libraries
  implementation 'io.grpc:grpc-protobuf:1.57.0'
  implementation 'io.grpc:grpc-services:1.57.0' // reflection
  implementation 'io.grpc:grpc-stub:1.57.0'
}

idea {
  module {
    sourceDirs += file("${buildDir}/generated/source/proto/main/java");
    sourceDirs += file("${buildDir}/generated/source/proto/main/grpc");
  }
}

protobuf {
  protoc {
    artifact = "com.google.protobuf:protoc:3.22.3"
  }
  plugins {
    grpc {
      artifact = 'io.grpc:protoc-gen-grpc-java:1.57.1'
    }
  }
  generateProtoTasks {
    all()*.plugins {
      grpc {}
    }
  }
}

spotless {
  java {
    palantirJavaFormat()
    importOrder("java|javax", "scala", "io.gatling", "", "\\#")
    removeUnusedImports()
    target project.fileTree(project.rootDir) {
      include '**/*.java'
      exclude 'build/generated/**/*.*'
    }
    trimTrailingWhitespace()
  }
}
